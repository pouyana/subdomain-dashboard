{% extends 'admin.twig' %}
{% block content %}
<h2 piwik-enriched-headline
    help-url="http://piwik.org/docs/manage-users/">Manage Subdomains Access</h2>
        <p>Here You can choose from the Domains you want their subdomains managed. The list of subdomains will be generated automaticaly.</p>  
    <div class="top_controls">
        {% include "@CoreHome/_siteSelectHeader.twig" %}
        {% include "@CoreHome/_periodSelect.twig" %}
    </div>
        <div class="entityContainer" style="margin-bottom:50px;margin-top:20px">
        <table class="entityTable dataTable" id="domains">
            <thead>
            <tr>
                <th>
                    Domain Name
                </th>
            </tr>
            <tbody>
                {% for domain in domains %}
                <tr>
                    <td id="domainName" class="editable">
                        {{ domain }}
                    </td>
                </tr>
                {% endfor %}
{% endblock %}
